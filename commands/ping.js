// const Discord = require("discord.js");
// const e = new Discord.Client();
module.exports = {
  name:'ping',
  args:false,
  description:'See if the bot\'s being a lazy bummer',
  execute(message,args){
    // const embed = new Discord.MessageEmbed()
    // .setColor("WHITE")
    // .setTitle("**Ping Pong**🏓")
    // .setDescription(`Latency is ${Date.now() - message.createdTimestamp}ms. API Latency is ${Math.round(e.ws.ping)}ms`)
   //  message.channel.send(embed);
  },
};